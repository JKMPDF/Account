
import React from 'react';
import PlaceholderPage from './PlaceholderPage';

const RecurringInvoices: React.FC = () => {
  // This is a placeholder for a complex feature.
  // A full implementation would require a form to create templates,
  // a list of existing templates, and a mechanism to generate vouchers from them.
  return (
    <PlaceholderPage title="Recurring Invoices" />
  );
};

export default RecurringInvoices;
